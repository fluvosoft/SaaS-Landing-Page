---
// Import necessary modules and utilities
import { Image } from "astro:assets";
import Icon from "@components/ui/icons/Icon.astro";
import type { CollectionEntry } from "astro:content";

const { product } = Astro.props;

interface Props {
  product: CollectionEntry<"products">;
}

// Define classes to be used with the Image component
const imageClass =
  "absolute inset-0 h-full w-full object-cover object-center transition duration-[600ms] ease-[cubic-bezier(0.45,0,0.55,1)] group-hover:scale-110";
---

<!-- Product card container following the format: image with name below -->
<div class="group md:col-span-2">
  <a
    href={product.data.externalUrl || `/products/${product.id}/`}
    target={product.data.externalUrl ? "_blank" : undefined}
    rel={product.data.externalUrl ? "noopener noreferrer" : undefined}
    data-astro-prefetch={!product.data.externalUrl}
    class="block overflow-hidden rounded-xl shadow-xl outline-hidden ring-zinc-500 focus-visible:ring-3 dark:ring-zinc-200 dark:focus:outline-hidden transition-transform duration-300 hover:shadow-2xl hover:-translate-y-1"
  >
    <!-- The product's main image -->
    <div class="relative h-48 overflow-hidden md:h-80">
      {typeof product.data.main.imgCard === 'string' ? (
        <img
          src={product.data.main.imgCard}
          alt={product.data.main.imgAlt}
          draggable={false}
          class="h-full w-full object-cover object-center transition-transform duration-500 group-hover:scale-110"
        />
      ) : (
        <Image
          src={product.data.main.imgCard}
          alt={product.data.main.imgAlt}
          draggable={"false"}
          class="h-full w-full object-cover object-center transition-transform duration-500 group-hover:scale-110"
        />
      )}
    </div>
    <!-- Product name and description below image -->
    <div class="bg-white dark:bg-neutral-800 px-5 py-5 md:px-6 md:py-6">
      <h3 class="text-xl font-bold text-neutral-900 dark:text-neutral-100 mb-2 transition-colors group-hover:text-orange-500 dark:group-hover:text-orange-400 md:text-2xl">
        {product.data.title}
      </h3>
      <p class="text-base text-neutral-600 dark:text-neutral-400 line-clamp-2 md:text-lg">
        {product.data.description}
      </p>
      <div class="mt-3 flex items-center text-sm text-neutral-500 dark:text-neutral-400">
        <span>View Details</span>
        <Icon name="openInNew" class="ml-2 h-4 w-4" />
      </div>
    </div>
  </a>
</div>
